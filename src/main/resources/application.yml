server:
    port: 8088
spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: ${ENTRA_ISSUER_URI}
                    audiences:
                        - ${ENTRA_API_CLIENT_ID}
microservice:
  base-url: ${MICROSERVICE_BASE_URL:http://localhost:8087}